services:
  db:
    image: postgres:latest
    container_name: postgres-local
    environment:
      POSTGRES_PASSWORD: Projeto_carteirinha
      POSTGRES_DB: db_carteirinha
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: